import { calculateKolkataFare } from './kolkataMetroData.js';

export function calculateFare(stationCount) {
  return calculateKolkataFare(stationCount);
}

export function calculateAquaFare(stationCount) {
  return calculateKolkataFare(stationCount);
}
